<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="dialog" parent="Theme.AppCompat.Dialog.Alert">

        <item name="android:colorBackground">@color/colorBackgroundFloating</item>
        <item name="android:textColorPrimary">#282727</item>
        <item name="android:textColorPrimaryInverse">#fff</item>
    </style>

    <color name="colorBackgroundFloating">#ffffff</color>/>

    <style name="Alert" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="windowNoTitle">true</item>
    </style>
</resources>
